networks:
  - 10.0.0.0/24
  - 192.168.1.0/24
scan_interval: 10s       # Fast monitoring interval for responsive IPs only
discovery_interval: 5m   # Slow discovery interval for full subnet sweeps
timeout: 1s
